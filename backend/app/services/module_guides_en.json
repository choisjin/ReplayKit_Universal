{
  "_meta": {
    "version": "2.0",
    "description": "Module function guide — Contains descriptions and parameter details for each function. Changes take effect without server restart."
  },
  "CANAT": {
    "_description": "CANat Device — Sends and receives CAN bus messages via CANat hardware",
    "functions": {
      "init": {
        "description": "Initializes and connects the CANat device.",
        "params": {
          "comport": "COM port. e.g. 'COM3'",
          "log_path": "Log file save path",
          "ch1_fd": "Channel 1 CAN FD mode. True (default) = FD, False = Classic",
          "ch2_fd": "Channel 2 CAN FD mode. True = FD, False (default) = Classic"
        }
      },
      "disconnect": {
        "description": "Disconnects from the CANat device.",
        "params": {}
      },
      "send_can_message": {
        "description": "Sends a CAN message.",
        "params": {
          "message_id": "CAN message ID",
          "cycle_time": "Transmission interval (ms). 0 for one-shot",
          "can_message": "Message data (hex)",
          "bus_channel": "CAN channel",
          "message_type": "'FD' (default) or 'Classic'"
        }
      },
      "CAN_PANEL": {
        "description": "Reaction-time visualization panel for CAN. With state=On and no signal it shows a black panel and waits; with state=On and a signal (message_id + can_message) it flashes the panel yellow while simultaneously sending send_can_message with no delay (film the panel with a camera and count frames from the yellow flash to the on-screen reaction to measure latency); with state=OFF it resets the panel to black only if it is already open (does nothing if not open — never spawns a new window); with state=Close it closes the panel.",
        "params": {
          "state": "Panel state. 'On' (show/flash+send), 'OFF' (reset to black only if already open, no spawn), 'Close' (close panel)",
          "message_id": "CAN message ID. When state=on and both this and can_message are set, it sends + flashes (black panel only if either is empty)",
          "cycle_time": "Transmission interval (ms). 0 for one-shot",
          "can_message": "Message data (hex)",
          "bus_channel": "CAN channel",
          "message_type": "'FD' (default) or 'Classic'",
          "x": "Panel top-left X (px). Empty = bottom-left default. Auto-filled by the 'Crop monitor' button",
          "y": "Panel top-left Y (px). Empty = bottom-left default",
          "width": "Panel width (px, default 300)",
          "height": "Panel height (px, default 300)"
        }
      },
      "stop_can_message": {
        "description": "Stops periodic transmission of a specific CAN message.",
        "params": {
          "message_id": "Message ID to stop",
          "cycle_time": "Cycle time",
          "can_message": "Message data",
          "bus_channel": "CAN channel",
          "message_type": "'FD' or 'Classic'"
        }
      },
      "send_cam_message_all_stop": {
        "description": "Stops all CAN message transmissions.",
        "params": {}
      },
      "can_message_save_start": {
        "description": "Starts saving CAN messages to a file.",
        "params": {
          "save_path": "Save path. Uses default path if empty"
        }
      },
      "can_message_save_stop": {
        "description": "Stops CAN message saving.",
        "params": {}
      }
    }
  },
  "CANoe_RBS": {
    "_description": "CANoe RBS control (py_canoe/COM) — Drives the CANoe application via COM to start/stop cfg (RBS)-based measurement, control signals and system/environment variables, and run UDS diagnostics.",
    "functions": {
      "Init_CANoe_Plugin": {
        "description": "Initializes CANoe and opens the cfg (kills existing CANoe processes, clears the win32com gen_py cache, initializes COM). Call this first before measurement/diagnostics.",
        "params": {
          "cfg_file": "CANoe cfg file path. e.g. 'D:/RBS/Cluster.cfg'",
          "canoe_log_dir": "py_canoe log folder. Default '.\\Log' (a timestamped subfolder is created automatically)"
        }
      },
      "Start": {
        "description": "Starts the measurement.",
        "params": {}
      },
      "Stop": {
        "description": "Stops the measurement.",
        "params": {}
      },
      "Quit": {
        "description": "Quits CANoe (force-kills the process on failure).",
        "params": {}
      },
      "SendSignal": {
        "description": "Sets a signal value of a message.",
        "params": {
          "bus": "Bus type. e.g. 'CAN'",
          "channel": "Channel number",
          "messageName": "Message name",
          "signalName": "Signal name",
          "value": "Value to set"
        }
      },
      "GetSignal": {
        "description": "Reads a signal value (returns only values received during measurement; otherwise errors).",
        "params": {
          "bus": "Bus type. e.g. 'CAN'",
          "channel": "Channel number",
          "messageName": "Message name",
          "signalName": "Signal name"
        }
      },
      "SetSysVar": {
        "description": "Sets a system variable value.",
        "params": {
          "namespace_name": "Namespace name",
          "sys_var_name": "System variable name",
          "value": "Value to set (int/float/str)"
        }
      },
      "GetSysVar": {
        "description": "Reads a system variable value (falls back to raw COM to surface the real cause when py_canoe returns None).",
        "params": {
          "namespace_name": "Namespace name",
          "sys_var_name": "System variable name",
          "return_symbolic_name": "Whether to return the symbolic name. Default False",
          "islog": "Whether to print a log. Default True"
        }
      },
      "CheckSysVar": {
        "description": "Compares a system variable value against an expected value (pass/fail).",
        "params": {
          "namespace_name": "Namespace name",
          "sys_var_name": "System variable name",
          "expect_value": "Expected value. Supports expressions: '>', '<', '~' (range), '|' (or), '&' (and)"
        }
      },
      "SetEnvVar": {
        "description": "Sets an environment variable value.",
        "params": {
          "env_var_name": "Environment variable name",
          "value": "Value to set (int/float/str/tuple)"
        }
      },
      "GetEnvVar": {
        "description": "Reads an environment variable value (use GetSysVar if it's a System Variables namespace variable).",
        "params": {
          "env_var_name": "Environment variable name",
          "islog": "Whether to print a log. Default True"
        }
      },
      "CheckEnvVar": {
        "description": "Compares an environment variable value against an expected value (pass/fail).",
        "params": {
          "env_var_name": "Environment variable name",
          "expect_value": "Expected value. Supports expressions: '>', '<', '~', '|', '&'"
        }
      },
      "DIAG_TestPresent": {
        "description": "Turns periodic Tester Present transmission on or off for the given ECU.",
        "params": {
          "diag_ecu_qualifier_name": "Diagnostic ECU qualifier name",
          "value": "True = activate, False = deactivate"
        }
      },
      "DIAG_Request": {
        "description": "Sends a UDS diagnostic request and returns the response.",
        "params": {
          "diag_ecu_qualifier_name": "Diagnostic ECU qualifier name",
          "request": "Request data. e.g. '10 01'",
          "request_in_bytes": "Interpret request as bytes. Default True",
          "return_sender_name": "Include sender name in the response. Default False",
          "timeout": "(unused — not supported by py_canoe)",
          "islog": "Whether to print a log. Default True"
        }
      },
      "CheckDIAG": {
        "description": "Sends a UDS diagnostic request and compares the response against an expected pattern (supports indexed comparison).",
        "params": {
          "diag_ecu_qualifier_name": "Diagnostic ECU qualifier name",
          "request": "Request data. e.g. '10 01'",
          "expect_value": "Expected response pattern. e.g. '50 01 00 32 01 F4'",
          "request_in_bytes": "Interpret request as bytes. Default True",
          "return_sender_name": "Include sender name in the response. Default False"
        }
      },
      "SendDiagMsg": {
        "description": "Sends a UDS diagnostic request, parses PRC/NRC, and returns a dict (retries on timeout).",
        "params": {
          "diag_ecu_qualifier_name": "Diagnostic ECU qualifier name",
          "request": "Request data. e.g. '10 01'",
          "timeout": "(unused — not supported by py_canoe)",
          "request_in_bytes": "Interpret request as bytes. Default True",
          "return_sender_name": "Include sender name in the response. Default False",
          "max_retries": "Maximum number of retries on timeout. Default 5",
          "retry_delay": "Delay between retries (seconds). Default 10.0"
        }
      }
    }
  },
  "CANoe_Ctrl": {
    "_description": "CANoe direct control (Vector) — Sends/receives CAN/CAN FD messages, saves ASC logs, and runs ISO-TP (UDS) diagnostics via Vector CAN hardware (python-can VectorBus). No CANoe application required.",
    "functions": {
      "canoe_full_log_save_start": {
        "description": "Starts saving all CAN traffic to an ASC log file (receives from all bus channels). Returns the final timestamped save path.",
        "params": {
          "path": "Log save folder path. e.g. 'D:/log'",
          "file_name": "Log file name (with extension). e.g. 'Auto.asc' — '_YYMMDD_HHMMSS' is appended automatically."
        }
      },
      "canoe_full_log_save_stop": {
        "description": "Stops full log saving and cleans up periodic transmit tasks.",
        "params": {}
      },
      "canoe_log_save_start": {
        "description": "Starts a section log (saves an additional section to a separate file while full logging is active).",
        "params": {
          "path": "Log save folder path",
          "file_name": "Log file name (with extension). e.g. 'section.asc'"
        }
      },
      "canoe_log_save_stop": {
        "description": "Stops the section log.",
        "params": {}
      },
      "canoe_send_message": {
        "description": "Sends a CAN/CAN FD message. A non-zero cycle time sends periodically; 0 sends once.",
        "params": {
          "message_id": "CAN ID (hex string). e.g. '0x288'",
          "cycle_time": "Transmit cycle (ms). 0 = send once",
          "can_message": "Message data (space-separated hex). e.g. '01 02 03 04 05 06 07 08'",
          "bus_channel": "Bus channel index (from 0, in device_info order)",
          "message_type": "'FD' (default) or any other string = Classic CAN"
        }
      },
      "canoe_send_msg_all_stop": {
        "description": "Stops all periodic transmits on the given channel.",
        "params": {
          "bus_channel": "Bus channel index"
        }
      },
      "find_msg_from_latest_log": {
        "description": "Searches a log file for a line containing a specific CAN ID and data. Returns ('pass', line) or ('fail', '').",
        "params": {
          "log_file": "Path of the log file to search",
          "message_id": "CAN ID to find. e.g. '0x288'",
          "can_message": "Data to check for inclusion (space-separated hex)"
        }
      },
      "canoe_set_diag_env": {
        "description": "Configures the ISO-TP (diagnostic) environment. Must be called once before sending/receiving diagnostics.",
        "params": {
          "bus_ch": "Bus channel index used for diagnostics",
          "tx_id": "Request (Tx) CAN ID (hex). e.g. '0x7E0'",
          "rx_id": "Response (Rx) CAN ID (hex). e.g. '0x7E8'",
          "can_type": "'FD' or 'CC' (Classic CAN, default)"
        }
      },
      "canoe_start_tester_present": {
        "description": "Starts sending periodic Tester Present (3E 00) messages (keeps the diagnostic session alive).",
        "params": {
          "interval": "Interval between messages (seconds). Default 2.0"
        }
      },
      "canoe_stop_tester_present": {
        "description": "Stops sending periodic Tester Present messages.",
        "params": {}
      },
      "canoe_send_diag_msg": {
        "description": "Sends a diagnostic (UDS) message via ISO-TP and evaluates the response (PRC/NRC). Returns a dict with status/response/service_id, etc. (requires canoe_set_diag_env first)",
        "params": {
          "can_message": "Diagnostic data to send (space-separated hex). e.g. '22 F1 90'",
          "timeout": "Response wait time (seconds). Default '5'",
          "wait_response": "Whether to wait for and evaluate the response. Default True"
        }
      }
    }
  },
  "IVIQEBenchIOClient": {
    "_description": "IVI QE Bench — Comprehensive control of relays, power, sensors, audio, LCD, buttons, and USB on the serial-connected QE test bench",
    "functions": {
      "Connect": {
        "description": "Connects to the bench via serial. Automatically called when adding the device.",
        "params": {}
      },
      "Disconnect": {
        "description": "Disconnects from the bench.",
        "params": {}
      },
      "ACCOnOff": {
        "description": "Turns ACC (Accessory) power on or off.",
        "params": {
          "state": "0 = OFF, 1 = ON",
          "bench_info": "Bench type. 'OLD_BENCH' (default) or 'NEW_BENCH'"
        }
      },
      "ACCSinglePulse": {
        "description": "Generates a single pulse on ACC power. Used for momentary voltage fluctuation testing.",
        "params": {
          "nVoltage": "Pulse voltage (V)",
          "nDelay": "Delay between pulses (ms)",
          "nCnt": "Number of pulse repetitions"
        }
      },
      "AccControl": {
        "description": "Controls the ACC relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON",
          "bench_info": "Bench type"
        }
      },
      "AccVoltage": {
        "description": "Sets the ACC voltage state.",
        "params": {
          "nState": "Voltage state value"
        }
      },
      "BatteryOnOff": {
        "description": "Turns battery power on or off.",
        "params": {
          "nState": "0 = OFF (disconnect battery), 1 = ON (connect battery)",
          "bench_info": "Bench type. 'OLD_BENCH' (default) or 'NEW_BENCH'"
        }
      },
      "BatteryControl": {
        "description": "Controls the battery relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON",
          "bench_info": "Bench type"
        }
      },
      "BatteryMinusControl": {
        "description": "Controls the battery negative (-) terminal relay.",
        "params": {
          "nStatus": "0 = OFF (connected), 1 = ON (disconnected)"
        }
      },
      "BatteryVoltage": {
        "description": "Sets the battery voltage state.",
        "params": {
          "nState": "Voltage state value"
        }
      },
      "BatterySinglePulse": {
        "description": "Generates a single pulse on the battery.",
        "params": {
          "nVoltage": "Pulse voltage (V)",
          "nDelay": "Delay between pulses (ms)",
          "nCnt": "Number of pulse repetitions"
        }
      },
      "IGNControl": {
        "description": "Controls the ignition (IGN) power.",
        "params": {
          "status": "0 = OFF, 1 = ON"
        }
      },
      "IGN3Control": {
        "description": "Controls the ignition 3 (IGN3) power.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "ALTControl": {
        "description": "Controls the alternator signal.",
        "params": {
          "status": "0 = OFF, 1 = ON"
        }
      },
      "ILLControl": {
        "description": "Controls the illumination signal (vehicle lighting interlock).",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "ILLControlMinus": {
        "description": "Controls the illumination negative signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "ReverseControl": {
        "description": "Controls the reverse gear signal.",
        "params": {
          "nStatus": "0 = OFF (forward), 1 = ON (reverse)"
        }
      },
      "ReverseActiveHigh": {
        "description": "Controls the reverse signal as Active High.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "ReverseActiveLow": {
        "description": "Controls the reverse signal as Active Low.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "ParkingControl": {
        "description": "Controls the parking signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "PPositonControl": {
        "description": "Controls the P (Park) gear position signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "NPositionControl": {
        "description": "Controls the N (Neutral) gear position signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "SpeedControl": {
        "description": "Controls the vehicle speed signal.",
        "params": {
          "nStatus": "0 = OFF (stopped), 1 = ON (driving)"
        }
      },
      "DoorControl": {
        "description": "Controls the door open/close signal.",
        "params": {
          "nStatus": "0 = OFF (closed), 1 = ON (open)"
        }
      },
      "DoorUnLockControl": {
        "description": "Controls the door unlock signal.",
        "params": {
          "nStatus": "0 = OFF (locked), 1 = ON (unlocked)"
        }
      },
      "TransDetectControl": {
        "description": "Controls the transmission detection signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "FUELControl": {
        "description": "Controls the fuel-related signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "KeylessBootingControl": {
        "description": "Controls the keyless (smart key) boot signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "AutolightControl": {
        "description": "Controls the auto-light signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "AlarmDetectControl": {
        "description": "Controls the alarm detection signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "AirbagCrashDetection": {
        "description": "Controls the airbag crash detection signal.",
        "params": {
          "model": "Vehicle model name",
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "ShieldBoxControl": {
        "description": "Controls the shield box (RF shielding box) open/close.",
        "params": {
          "nStatus": "0 = OFF (close), 1 = ON (open)"
        }
      },
      "ANTControl": {
        "description": "Controls the antenna relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "AmpControl": {
        "description": "Controls the external audio amplifier power.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "MICControl": {
        "description": "Controls the microphone relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "AUXControl": {
        "description": "Controls the AUX input relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "CDControl": {
        "description": "Controls the CD drive relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "IPODControl": {
        "description": "Controls the iPod connection relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "RcInControl": {
        "description": "Controls the RC input relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "ComEnableControl": {
        "description": "Controls the COM (communication) enable.",
        "params": {
          "nStatus": "0 = OFF (disabled), 1 = ON (enabled)"
        }
      },
      "Detent": {
        "description": "Controls the detent (rotary encoder for volume knob, etc.) signal.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "AVNSDDetect": {
        "description": "Controls the AVN SD card detection signal.",
        "params": {
          "nStatus": "0 = OFF (not inserted), 1 = ON (inserted)"
        }
      },
      "USB1Control": {
        "description": "Controls the USB 1 port relay.",
        "params": {
          "Status": "0 = OFF, 1 = ON"
        }
      },
      "USBFrontSwitchControl": {
        "description": "Controls the front USB switch relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "USBRearSwitchControl": {
        "description": "Controls the rear USB switch relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "USBSelectorControl": {
        "description": "Selects a USB selector port.",
        "params": {
          "nOnPortNum": "USB port number to activate"
        }
      },
      "USBSelector10CHSwitch5CH": {
        "description": "Controls the USB selector 10-channel/5-channel switch.",
        "params": {
          "nType": "Switch type",
          "nPortNum": "Port number",
          "nOnOffStatus": "0 = OFF, 1 = ON"
        }
      },
      "Telltale": {
        "description": "Controls the telltale (dashboard warning indicator) relay.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "TelltaleLED": {
        "description": "Sets the telltale LED blink period.",
        "params": {
          "nPeriod": "Blink period (ms)"
        }
      },
      "CheckTelltale": {
        "description": "Checks the telltale (warning indicator) status.",
        "params": {}
      },
      "LEDPulse": {
        "description": "Sets the LED blink period.",
        "params": {
          "nPeriod": "Blink period (ms)"
        }
      },
      "LedTCU4": {
        "description": "Sets the TCU4 LED blink period.",
        "params": {
          "nPeriod": "Blink period (ms)"
        }
      },
      "CheckLedTCU4": {
        "description": "Checks the TCU4 LED status.",
        "params": {}
      },
      "GetLEDPulse": {
        "description": "Reads the current LED pulse value.",
        "params": {}
      },
      "CheckLCD": {
        "description": "Checks the LCD display status.",
        "params": {
          "nStatus": "Status value to check"
        }
      },
      "CheckLCDDimming": {
        "description": "Checks the LCD dimming (brightness control) status.",
        "params": {
          "nStatus": "Status value to check"
        }
      },
      "SetLCDValue": {
        "description": "Sets the LCD value.",
        "params": {
          "nLCDValue": "LCD value to set"
        }
      },
      "GetLCDValue": {
        "description": "Reads the current LCD value.",
        "params": {}
      },
      "CheckVoltage": {
        "description": "Checks whether voltage is within the specified range.",
        "params": {
          "nMin": "Minimum voltage (V)",
          "nMax": "Maximum voltage (V)"
        }
      },
      "CheckCurrent": {
        "description": "Checks whether current is within the specified range.",
        "params": {
          "nMin": "Minimum current (A)",
          "nMax": "Maximum current (A)"
        }
      },
      "ExtCheckCurrent": {
        "description": "Checks whether external current is within the specified range.",
        "params": {
          "fMin": "Minimum current (A)",
          "fMax": "Maximum current (A)"
        }
      },
      "EXT_CURRENT": {
        "description": "Checks external current within range after a delay.",
        "params": {
          "min_current": "Minimum current (A)",
          "max_current": "Maximum current (A)",
          "check_delay": "Delay before check (seconds)"
        }
      },
      "GetEXTCurrent": {
        "description": "Reads the current external current value.",
        "params": {}
      },
      "CheckEXTCurrentList": {
        "description": "Checks whether collected external current values are within range.",
        "params": {
          "nMin": "Minimum current (A)",
          "nMax": "Maximum current (A)"
        }
      },
      "ClearEXTCurrentList": {
        "description": "Clears the collected external current list.",
        "params": {}
      },
      "CheckKeyOutVoltage": {
        "description": "Checks whether key output voltage is within range.",
        "params": {
          "nMin": "Minimum voltage (V)",
          "nMax": "Maximum voltage (V)"
        }
      },
      "ClearKeyOutVoltageList": {
        "description": "Clears the key output voltage list.",
        "params": {}
      },
      "CheckSound": {
        "description": "Checks the audio output status.",
        "params": {
          "nStatus": "Status value to check"
        }
      },
      "PlaySound": {
        "description": "Plays a sound file on the bench.",
        "params": {
          "strFileName": "File name to play",
          "nFileNameLen": "File name length"
        }
      },
      "InitSoundUpdate": {
        "description": "Initializes the sound update.",
        "params": {}
      },
      "WaitSoundUpdate": {
        "description": "Waits until the sound update completes.",
        "params": {
          "timeout": "Max wait time in seconds"
        }
      },
      "RequestLightSensorControl": {
        "description": "Controls the light sensor.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON",
          "pbData": "PB data",
          "twData": "TW data"
        }
      },
      "GetLightSensorData": {
        "description": "Reads light sensor data.",
        "params": {}
      },
      "CheckLightSensorResult": {
        "description": "Checks the light sensor result.",
        "params": {}
      },
      "TenButton": {
        "description": "Presses a button on the 10-button panel.",
        "params": {
          "nButton": "Button number (0-9)",
          "nDelay": "Button press duration (ms)"
        }
      },
      "ThreeButton": {
        "description": "Presses a button on the 3-button panel.",
        "params": {
          "nButton": "Button number (0-2)",
          "nDelay": "Button press duration (ms)"
        }
      },
      "ThreeButton_Front": {
        "description": "Presses a button on the front 3-button panel.",
        "params": {
          "nButton": "Button number (0-2)",
          "nDelay": "Button press duration (ms)"
        }
      },
      "ThreeButton_Rear": {
        "description": "Presses a button on the rear 3-button panel.",
        "params": {
          "nButton": "Button number (0-2)",
          "nDelay": "Button press duration (ms)"
        }
      },
      "TwoButton": {
        "description": "Presses a button on the 2-button panel.",
        "params": {
          "nButton": "Button number (0-1)",
          "nDelay": "Button press duration (ms)"
        }
      },
      "rf_btn_press": {
        "description": "Presses an RF button.",
        "params": {
          "btnNum": "Button number",
          "delay": "Press duration (ms)"
        }
      },
      "FileToSD": {
        "description": "Copies a file to the SD card.",
        "params": {
          "userFilePath": "Source file path on PC",
          "sdCardFileName": "File name to save on SD card"
        }
      },
      "Relay_10": {
        "description": "Controls relay 10.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "Relay_11": {
        "description": "Controls relay 11.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "Relay_12": {
        "description": "Controls relay 12.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "Relay_13": {
        "description": "Controls relay 13.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "Relay_14": {
        "description": "Controls relay 14.",
        "params": {
          "nStatus": "0 = OFF, 1 = ON"
        }
      },
      "RelayInitialization": {
        "description": "Resets all relays to their default values.",
        "params": {
          "init_value": "Initialization value"
        }
      },
      "SetRelayConfig": {
        "description": "Sets the relay configuration.",
        "params": {
          "nRelayCfg": "Relay configuration value"
        }
      },
      "CallRelayFunc": {
        "description": "Calls a relay function by index.",
        "params": {
          "func_index": "Function index number",
          "data": "Data to pass"
        }
      },
      "RequestRelayInitialStatus": {
        "description": "Requests the initial relay status.",
        "params": {}
      },
      "RequestRelaySet": {
        "description": "Sets relays using a command list.",
        "params": {
          "nCmdList": "Relay command list"
        }
      },
      "SystemReset": {
        "description": "Resets the bench system.",
        "params": {
          "nMs": "Wait time after reset (ms). Default: 1000"
        }
      },
      "SetSystemTime": {
        "description": "Sets the bench system time to the current PC time.",
        "params": {}
      },
      "RequestSystemVersion": {
        "description": "Requests the bench system version information.",
        "params": {}
      },
      "CalcCRC16": {
        "description": "Calculates a CRC-16 checksum. Used for communication data verification.",
        "params": {
          "listData": "Data list",
          "nLen": "Data length"
        }
      },
      "CalibrationStartOrStop": {
        "description": "Starts or stops calibration.",
        "params": {
          "sensitivity": "Sensitivity value",
          "threshold": "Threshold value"
        }
      },
      "AnnouncesAutoTestStartOrEnd": {
        "description": "Announces the start/end of an automated test.",
        "params": {
          "bisStart": "True = test start, False = test end"
        }
      },
      "BATSwitchSETtoPC": {
        "description": "Switches the battery switch from SET to PC.",
        "params": {}
      },
      "BATSwitchUSBtoPC": {
        "description": "Switches the battery switch from USB to PC.",
        "params": {}
      },
      "RequestAudioFrequency": {
        "description": "Requests audio frequency measurement.",
        "params": {
          "bStart": "True = start, False = stop",
          "nMs": "Measurement interval (ms)"
        }
      },
      "RequestAudioStatusAndADCValue": {
        "description": "Requests audio status and ADC value.",
        "params": {
          "nMs": "Response wait time (ms)"
        }
      },
      "RequestAudioStatusAndADCValuePeriodicallyNoti": {
        "description": "Receives periodic notifications of audio status and ADC value.",
        "params": {
          "bStart": "True = start, False = stop",
          "nPeriodMs": "Notification interval (ms)"
        }
      },
      "RequestAudioStatusPeriodicallyNoti": {
        "description": "Receives periodic notifications of audio status.",
        "params": {
          "bStart": "True = start, False = stop",
          "nPeriodMs": "Notification interval (ms)"
        }
      },
      "RequestCurrentPeriodicallyNoti": {
        "description": "Receives periodic notifications of current value.",
        "params": {
          "bStart": "True = start, False = stop",
          "nPeriodMs": "Notification interval (ms)"
        }
      },
      "RequestExtCurrentPeriodicallyNoti": {
        "description": "Receives periodic notifications of external current value.",
        "params": {
          "bStart": "True = start, False = stop",
          "nPeriodMs": "Notification interval (ms)"
        }
      },
      "RequestVoltagePeriodicallyNoti": {
        "description": "Receives periodic notifications of voltage value.",
        "params": {
          "bStart": "True = start, False = stop",
          "nPeriodMs": "Notification interval (ms)"
        }
      },
      "RequestKeyOutVoltagePeriodicallyNoti": {
        "description": "Receives periodic notifications of key output voltage.",
        "params": {
          "bStart": "True = start, False = stop",
          "nPeriodMs": "Notification interval (ms)"
        }
      },
      "RequestLCDStatus": {
        "description": "Requests LCD status.",
        "params": {
          "bStart": "True = start, False = stop",
          "nMs": "Response wait time (ms)"
        }
      },
      "RequestLCDIllumiSensorValue": {
        "description": "Requests LCD illumination sensor value.",
        "params": {
          "bStart": "True = start, False = stop",
          "nMs": "Response wait time (ms)"
        }
      },
      "RequestLCDStatusAndIllumiSensorValuePeriodicallyNoti": {
        "description": "Receives periodic notifications of LCD status and illumination sensor value.",
        "params": {
          "bStart": "True = start, False = stop",
          "nPeriodMs": "Notification interval (ms)"
        }
      },
      "RequestSoundFileInfo": {
        "description": "Requests sound file information.",
        "params": {
          "nSoundType": "Sound type"
        }
      },
      "RequestSoundFileList": {
        "description": "Requests the sound file list.",
        "params": {
          "nSoundType": "Sound type"
        }
      },
      "Read": {
        "description": "Reads data from the bench.",
        "params": {}
      },
      "Send": {
        "description": "Sends raw byte data to the bench.",
        "params": {
          "listData": "Data list to send",
          "nLen": "Data length"
        }
      },
      "WriteRawString": {
        "description": "Sends a raw string to the bench.",
        "params": {
          "string": "String to send"
        }
      },
      "ReceiveStart": {
        "description": "Starts or stops bench data reception.",
        "params": {
          "bStart": "True = start receiving, False = stop receiving",
          "queue": "Queue to store received data (optional)"
        }
      },
      "timer_compare": {
        "description": "Compares timer values.",
        "params": {
          "Time_Init": "Start time",
          "Time_Check": "Time to compare"
        }
      }
    }
  },
  "SSHManager": {
    "_description": "SSH Connection — Connects to remote devices via SSH for command execution and file transfer",
    "functions": {
      "create_ssh_client": {
        "description": "Creates an SSH connection.",
        "params": {
          "hostname": "Remote device IP or hostname. e.g. '192.168.1.100'",
          "username": "SSH username. e.g. 'root'",
          "password": "SSH password",
          "key_filePath": "SSH key file path (alternative to password). Default: None"
        }
      },
      "close_ssh_client": {
        "description": "Closes the SSH connection.",
        "params": {}
      },
      "send_command": {
        "description": "Executes a command via SSH and returns the full output on completion (blocking). Korean Windows output is automatically handled via cp949/euc-kr encoding. Writing the command as 'bash -s < <local path>' auto-injects that local (PC) file as stdin, running a local script remotely without uploading.",
        "params": {
          "command": "Command to execute. e.g. 'ls -al', 'cat /etc/version'. Writing it as 'bash -s < <local path>' auto-injects that local (PC) file as stdin so a local script runs remotely without uploading. e.g. 'sudo bash -s < C:\\ReplayKit\\scripts\\Check_Battery.sh'"
        }
      },
      "send_command_stream": {
        "description": "Executes a command via SSH and immediately returns a [BG_TASK:bg_x] placeholder (non-blocking). Output streams in real time and can be observed via the polling endpoint. Useful for long-running commands (netstat, tail -f, etc.).",
        "params": {
          "command": "Command to execute. e.g. 'netstat -a', 'tail -f /var/log/syslog'"
        }
      },
      "Check": {
        "description": "Executes a command via SSH and compares the output against an expected value (blocking). Returns a 'FAIL:' prefix on failure so the step is judged as failed. Korean output is automatically handled via cp949/euc-kr encoding. Writing the command as 'bash -s < <local path>' auto-injects that local (PC) file as stdin, running a local script remotely without uploading.",
        "params": {
          "command": "Command to execute. Writing it as 'bash -s < <local path>' auto-injects that local (PC) file as stdin so a local script runs remotely without uploading. e.g. 'sudo bash -s < C:\\ReplayKit\\scripts\\Check_Battery.sh'",
          "expected": "Expected string that must be contained in or exactly match the output. Leave empty for 'pass only when there is no output' (no-output check)",
          "match_mode": "Comparison mode: 'contains' (substring) or 'exact' (full match). Default: contains",
          "timeout": "Maximum wait time (seconds). Default: 60s"
        }
      },
      "Check_Logic": {
        "description": "Executes a command via SSH and judges pass/fail against multiple keywords using and/or logic (blocking). Returns a 'FAIL:' prefix on failure so the step is judged as failed. Writing the command as 'bash -s < <local path>' auto-injects that local (PC) file as stdin, running a local script remotely without uploading.",
        "params": {
          "command": "Command to execute. Writing it as 'bash -s < <local path>' auto-injects that local (PC) file as stdin so a local script runs remotely without uploading. e.g. 'sudo bash -s < C:\\ReplayKit\\scripts\\Check_Battery.sh'",
          "keywords": "Comma-separated keyword list. e.g. 'OK,ready,done'",
          "logic": "Logic: 'and' (pass when all keywords present) or 'or' (pass when at least one present). Default: and",
          "timeout": "Maximum wait time (seconds). Default: 60s"
        }
      },
      "send_file": {
        "description": "Transfers a local file to the remote device.",
        "params": {
          "local_path": "File path on PC",
          "remote_path": "Destination path on remote device"
        }
      },
      "get_file": {
        "description": "Downloads a file from the remote device to the PC.",
        "params": {
          "remote_path": "File path on remote device",
          "local_path": "Destination path on PC"
        }
      },
      "get_folder": {
        "description": "Recursively downloads an entire folder (including subfolders) from the remote device to the PC via SFTP. Non-regular files such as symlinks are skipped. If some files fail, returns a 'FAIL:' prefix so the step is judged as fail.",
        "params": {
          "remote_path": "Folder path on remote device. e.g. '/var/log'",
          "local_path": "Destination folder path on PC. If empty during playback, saved automatically under the run folder's logs/ with the remote folder name"
        }
      },
      "get_file_list_in_dir": {
        "description": "Lists files in a directory on the remote device.",
        "params": {
          "path": "Directory path to list",
          "_filter": "File filter pattern. Default: '*' (all). e.g. '*.log'"
        }
      }
    }
  },
  "DLTLogging": {
    "_description": "DLT Logging — Captures and saves logs via direct TCP connection to the DLT daemon, with keyword-based PASS/FAIL evaluation (no GUI required)",
    "functions": {
      "StartLogging": {
        "description": "Connects to DLT and starts capture. Streams all logs immediately to a text file (auto path) while keeping only recent entries in memory (stays fast even for long runs). The viewer modal opens automatically.",
        "params": {}
      },
      "StopLogging": {
        "description": "Stops DLT capture. Logs are already saved to file in real time; specifying save_path only moves the file to that location.",
        "params": {
          "save_path": "Save file path. If empty, keeps the streaming file as-is (during replay: run_dir/logs/dlt_*.log, step test: backend/results/Temp_logs/dlt_*.log)"
        }
      },
      "WaitLog": {
        "description": "Waits (blocking) until a log containing the keyword appears. Returns FAIL on timeout.",
        "params": {
          "keyword": "Search keyword (space-separated = AND condition)",
          "timeout": "Max wait time in seconds (default: 30)"
        }
      },
      "ExpectFound": {
        "description": "Periodically searches the entire log until the keyword is found. Searches immediately, then retries at intervals (timeout / max_retries). Returns PASS if found, FAIL if not found after all retries.",
        "params": {
          "keyword": "Search keyword (space-separated = AND condition)",
          "timeout": "Total wait time in seconds (default: 60)",
          "max_retries": "Maximum retry count (default: 5). Interval = timeout / max_retries"
        }
      },
      "ExpectNotFound": {
        "description": "Periodically checks the entire log to confirm the keyword is absent. Returns FAIL immediately if found. Rechecks at intervals (timeout / max_retries). Returns PASS if not found after all checks.",
        "params": {
          "keyword": "Search keyword (space-separated = AND condition)",
          "timeout": "Total check time in seconds (default: 60)",
          "max_retries": "Maximum check count (default: 5). Interval = timeout / max_retries"
        }
      }
    }
  },
  "SerialLogging": {
    "_description": "Serial Logging — Captures serial port logs with keyword-based fail detection.\n\n**Connection lifecycle**: The serial port itself is opened/closed by the Device tab's Connect/Disconnect. StartLogging does NOT reopen an already-open port, so devices that reset their settings on every connect are safe.\n\n**⚠️ Usage: StartLogging and StopLogging are paired.**\n- Without StopLogging the memory buffer is never written to a file.\n- StopLogging ends the capture session but **keeps the serial port open**. To close the port, use the Device tab Disconnect.\n- On abnormal scenario exit, cleanup auto-saves any unsaved buffer during Disconnect.\n\nStandard usage pattern:\n  1) StartLogging()                              ← first step (capture starts; port pre-opened via Device tab Connect)\n  2) SendCommand / SendCommand_*_on_keyword ...  ← middle steps\n  3) StopLogging()                               ← last step (writes log file + ends capture, port stays open)",
    "functions": {
      "StartLogging": {
        "description": "Starts serial capture (memory only, no file save). Viewer modal opens automatically. **If the port is already open, no reconnect happens** — only the capture session is (re)started, so devices that reset on connect are safe. Returns once the capture thread is ready, so the next step's SendCommand is not dropped.\n\n**⚠️ Must be paired with StopLogging.** Without StopLogging the in-memory buffer is never flushed to disk.",
        "params": {
          "settle_ms": "Port stabilization wait after open (ms). Default 500. **Skipped if the port is already open.** Used only on the first open to ensure USB-Serial driver reset/buffer settle. Use 1500~2000 for DTR-reset boards (Arduino)."
        }
      },
      "StopLogging": {
        "description": "Writes the memory buffer to a file and ends the capture session. **The serial port is kept open** — subsequent SendCommand calls will not re-initialize the device. To close the port, use the Device tab Disconnect.\n\n**⚠️ Must be paired with StartLogging.** This call is what:\n  • Flushes the in-memory log buffer to a .log file on disk\n  • Tears down the capture thread to avoid resource leaks\n  • Allows the next StartLogging to begin a fresh session\nEven if the file-save step throws, the finally block guarantees the capture is torn down.",
        "params": {
          "save_path": "Save file path. Auto if empty (during playback: run_dir/logs/serial_*.log, step test: backend/results/Temp_logs/serial_*.log)"
        }
      },
      "SendCommand": {
        "description": "Sends a text command via the serial port.",
        "params": {
          "command": "Command to send. e.g. 'reboot', 'cat /proc/version'",
          "encoding": "Encoding (default: utf-8)",
          "append_newline": "Auto-append newline. true/false (default: true)"
        }
      },
      "Send_Packet": {
        "description": "Sends a raw hex byte packet over the serial port. Each space-separated hex token is converted to a single byte and sent, then flushed to guarantee it reaches the wire (1-digit / 2-digit / 0x-prefixed tokens are all accepted).",
        "params": {
          "data": "Space-separated hex string. e.g. '79 6D F2 0F' or '00 77 42 37 02 F2 00 FE'"
        }
      },
      "SendCommand_fail_on_keyword": {
        "description": "Sends a command, then **FAILS if the keyword appears** in the response. Used to detect abnormal keywords like 'ERROR'/'Fail'/'crash'. After sending, monitors only newly captured lines for 'time' seconds; every matching line is appended as a fail row and displayed inline (Fail_Count_N). Returns PASS if no match.",
        "params": {
          "command": "Command to send. e.g. 'status', 'self_test'",
          "keyword": "Substring that triggers FAIL. e.g. 'ERROR', 'Fail', 'crash'",
          "time": "Response monitoring duration (seconds). Default 5",
          "encoding": "Encoding (default: utf-8)",
          "append_newline": "Auto-append newline. true/false (default: true)"
        }
      },
      "SendCommand_pass_on_keyword": {
        "description": "Sends a command, then **PASSES if the keyword appears** in the response. Used for normal-response keywords like 'OK'/'Pass'/'BootComplete'. Returns PASS immediately on first match (early exit). If not found within 'time' seconds, appends one fail row and returns FAIL.",
        "params": {
          "command": "Command to send. e.g. 'ping', 'getversion'",
          "keyword": "Substring required for PASS. e.g. 'OK', 'Pass', 'BootComplete'",
          "time": "Response wait duration (seconds). Default 5",
          "encoding": "Encoding (default: utf-8)",
          "append_newline": "Auto-append newline. true/false (default: true)"
        }
      },
      "Monitor_pass_on_keyword": {
        "description": "WITHOUT sending any command (no write), PASS if keyword is found in the log. If include_past=True, first scans logs captured so far, then monitors new lines for `time` seconds; PASS on first match. Sends nothing (not even a newline), so it does not disturb a webcam on the same USB hub.",
        "params": {
          "keyword": "Keyword to PASS on (substring match)",
          "time": "Seconds to wait for future log lines. Default 5. 0 = scan past buffer only",
          "include_past": "Whether to also scan logs captured so far. true/false (default true)"
        }
      },
      "Monitor_fail_on_keyword": {
        "description": "WITHOUT sending any command (no write), FAIL if keyword is found in the log. For monitoring abnormal keywords (ERROR/crash). Scans past buffer (if include_past) and new lines for `time` seconds, accumulating all matches.",
        "params": {
          "keyword": "Keyword that triggers FAIL (substring match)",
          "time": "Seconds to monitor future log lines. Default 5. 0 = scan past buffer only",
          "include_past": "Whether to also scan logs captured so far. true/false (default true)"
        }
      }
    }
  },
  "SmartBench": {
    "_description": "Smart Bench TCP Control — Controls power, buttons, current, and LEDs on Smart Bench equipment via TCP socket",
    "functions": {
      "Connect": {
        "description": "Connects to the Smart Bench via TCP.",
        "params": {}
      },
      "Disconnect": {
        "description": "Disconnects from the Smart Bench.",
        "params": {}
      },
      "Battery": {
        "description": "Controls the Battery (+B) relay.",
        "params": {
          "status": "'on' or 'off'"
        }
      },
      "ACC": {
        "description": "Controls the ACC relay.",
        "params": {
          "status": "'on' or 'off'"
        }
      },
      "IGN": {
        "description": "Controls the IGN relay.",
        "params": {
          "status": "'on' or 'off'"
        }
      },
      "IGN3": {
        "description": "Controls the IGN3 relay.",
        "params": {
          "status": "'on' or 'off'"
        }
      },
      "USB": {
        "description": "Controls the USB relay.",
        "params": {
          "status": "'on' or 'off'"
        }
      },
      "CheckCurrent": {
        "description": "Measures current and returns PASS if within range for 3 consecutive readings, FAIL on timeout.",
        "params": {
          "min_current": "Minimum current (A). e.g. 0.5",
          "max_current": "Maximum current (A). e.g. 2.0",
          "check_delay": "Timeout (ms). e.g. 10000"
        }
      },
      "CheckCurrentMoment": {
        "description": "Measures instantaneous current and returns PASS if within range, FAIL if out of range for 3 consecutive readings.",
        "params": {
          "min_current": "Minimum current (A)",
          "max_current": "Maximum current (A)"
        }
      },
      "CheckCurrentMaintain": {
        "description": "Verifies that current stays within range for the specified duration.",
        "params": {
          "min_current": "Minimum current (A)",
          "max_current": "Maximum current (A)",
          "check_delay": "Maintenance duration (ms)"
        }
      },
      "ButtonPress": {
        "description": "Presses a button.",
        "params": {
          "button_num": "Button number (0=ECALL, etc.)",
          "press_time": "Press duration (ms). e.g. 3000"
        }
      },
      "LEDCheck": {
        "description": "Verifies the LED status.",
        "params": {
          "data": "LED verification data. e.g. 'honda;4.0;GREEN;BLINK;RED;ON'"
        }
      },
      "Relay": {
        "description": "Directly controls a relay.",
        "params": {
          "relay_num": "Relay number",
          "status": "'on' or 'off'"
        }
      },
      "SendRaw": {
        "description": "Sends a raw text command directly.",
        "params": {
          "command": "Command string to send"
        }
      },
      "GetStatus": {
        "description": "Returns the current module status.",
        "params": {}
      }
    }
  },
  "WoohyunBench": {
    "_description": "Woohyun Bench — Integrated UDP test bench: vehicle power control (IGN/ACC/Battery) + CAN FD signal transmission",
    "functions": {
      "Connect": {
        "description": "Establishes a UDP network connection to the bench equipment. Automatically called when adding the device, so manual invocation is usually unnecessary.",
        "params": {}
      },
      "Disconnect": {
        "description": "Disconnects the UDP network connection from the bench equipment.",
        "params": {}
      },
      "IsConnected": {
        "description": "Checks whether the bench equipment is currently connected.",
        "params": {}
      },
      "IGN1": {
        "description": "Turns IGN1 (Ignition 1) power on or off. Corresponds to the vehicle's ignition power.",
        "params": {
          "on_off": "0 = OFF, 1 = ON. Default: 1 (ON)"
        }
      },
      "IGN1_Read": {
        "description": "Reads the current IGN1 power state. Returns: 0=OFF, 1=ON, -1=read failure",
        "params": {}
      },
      "IGN2": {
        "description": "Turns IGN2 (Ignition 2) power on or off. Corresponds to auxiliary ignition power.",
        "params": {
          "on_off": "0 = OFF, 1 = ON. Default: 1 (ON)"
        }
      },
      "IGN2_Read": {
        "description": "Reads the current IGN2 power state. Returns: 0=OFF, 1=ON, -1=read failure",
        "params": {}
      },
      "ACC": {
        "description": "Turns ACC (Accessory) power on or off. Corresponds to the vehicle's accessory power (radio, navigation, etc.).",
        "params": {
          "on_off": "0 = OFF, 1 = ON. Default: 1 (ON)"
        }
      },
      "ACC_Read": {
        "description": "Reads the current ACC power state. Returns: 0=OFF, 1=ON, -1=read failure",
        "params": {}
      },
      "BATTERY": {
        "description": "Turns the battery relay on or off. When OFF, battery power is completely cut from the vehicle.",
        "params": {
          "on_off": "0 = OFF (disconnect battery), 1 = ON (connect battery). Default: 1 (ON)"
        }
      },
      "BATTERY_Read": {
        "description": "Reads the current battery relay state. Returns: 0=OFF, 1=ON, -1=read failure",
        "params": {}
      },
      "BatterySet": {
        "description": "Sets the battery output voltage. Used for voltage fluctuation testing.",
        "params": {
          "voltage": "Voltage to set (V, decimal allowed). e.g. 12.0, 14.4, 9.0. Default: 14.4V"
        }
      },
      "BatteryCheck": {
        "description": "Measures the current battery output voltage. Returns: voltage (V), -1.0 on failure",
        "params": {}
      },
      "AmpereCheck": {
        "description": "Measures the current power consumption. Returns: current (A), -1.0 on failure",
        "params": {}
      },
      "GetInfo": {
        "description": "Returns the bench's current connection information (connected MCUs, signal_file, connection state).",
        "params": {}
      },
      "LoadSignals": {
        "description": "Loads CAN FD signal definitions from an Excel (.xls/.xlsx) or XML (.CAN) file at runtime.",
        "params": {
          "file_path": "Absolute path to the signal definition file"
        }
      },
      "SendSignal": {
        "description": "Sends a named CAN FD signal with the given physical value. Repeats 5 times at 200ms interval. Requires signal definitions to be loaded.",
        "params": {
          "signal_name": "Signal name (e.g. 'Warn_DrvDrSwSta')",
          "physical_value": "Physical value (int/float/hex string like '0xAB')"
        }
      },
      "DoorTest": {
        "description": "Cycles the driver door switch signal (Warn_DrvDrSwSta) ON/OFF to verify door handling.",
        "params": {}
      },
      "TestAllSignals": {
        "description": "Sends every loaded CAN FD signal with its midpoint value. Useful for load/sanity testing.",
        "params": {}
      },
      "SendTimeSync": {
        "description": "Broadcasts a time-sync packet over UDP to all bench equipment. Automatically called on Connect, so manual invocation is usually unnecessary.",
        "params": {}
      },
      "SendCan": {
        "description": "Sends a raw CAN frame. Transmits a standard/extended/FD frame to the given device (mcu) and channel, either once or periodically.",
        "params": {
          "mcu": "Target MCU (default: 'mcu1' / e.g. 'mcu1', 'mcu2', 'mcu3')",
          "msg_id": "CAN ID. Integer or hex string (parsed as hex regardless of '0x' prefix. e.g. '0x33E', '47A')",
          "type": "Frame type (default: 'STA' / 'STA'=standard, 'EXT'=extended, 'FD'=CAN FD)",
          "payload_hex": "Payload hex bytes (spaces/commas allowed, no brackets. e.g. '00,00,00,00,00,C0,00,00' or 'FF 01 02')",
          "channel": "Transmit channel (default: 'A' / e.g. 'A', 'B', 'C', 'D')",
          "repeat": "Send mode (default: 0 / 0=single shot, 1=periodic)",
          "cycle_ms": "Send interval in ms when repeat=1 (default: 200)"
        }
      },
      "SendStopCan": {
        "description": "Stops the periodic transmission of a specific Raw CAN message corresponding to the provided msg_id.",
        "params": {
          "mcu": "Target MCU to control (default: 'mcu1' / e.g. 'mcu1', 'mcu2', 'mcu3')",
          "msg_id": "Target CAN ID to stop (integer or hex string. Parsed as hex regardless of the '0x' prefix. e.g. '0x33E', '47A')"
        }
      },
      "SendAllStopCan": {
        "description": "Stops all active periodic Raw CAN message transmissions across all connected MCUs simultaneously.",
        "params": {}
      },
      "CanSaveStart": {
        "description": "Starts unified real-time logging of Rx/Tx CAN data from every connected MCU/channel into a single CSV file.",
        "params": {
          "save_dir": "CSV save folder path. Defaults to ./canlog when empty (filename CAN_LOG_<timestamp>.csv is auto-generated)"
        }
      },
      "CanSaveStop": {
        "description": "Stops unified CAN logging and closes the CSV file.",
        "params": {}
      }
    }
  },
  "CMD": {
    "_description": "Command Execution — Executes Windows command prompt (CMD) commands. Used for running external programs or system commands",
    "functions": {
      "Run": {
        "description": "Executes a command and waits for completion (blocking). Returns the result.",
        "params": {
          "command": "Command to execute. e.g. 'ping 127.0.0.1 -n 3', 'ipconfig'",
          "timeout": "Max wait time in seconds. Default: 300"
        }
      },
      "Check": {
        "description": "Executes a command and verifies the output against an expected string (blocking). Returns a 'FAIL:' prefix on mismatch, which causes the step to be marked as fail.",
        "params": {
          "command": "Command to execute",
          "expected": "Expected substring or full match value",
          "match_mode": "Comparison mode: 'contains' (substring) or 'exact' (full match). Default: contains",
          "timeout": "Max wait time in seconds. Default: 300"
        }
      },
      "RunCapture": {
        "description": "Executes a command in the background and immediately returns a '[BG_TASK:bg_x]' placeholder (non-blocking). The actual output can be retrieved later via the polling endpoint.",
        "params": {
          "command": "Command to execute"
        }
      },
      "CheckCapture": {
        "description": "Executes a command in the background and returns a '[BG_TASK:bg_x]' placeholder. On completion, the server evaluates expected/match_mode to determine the final pass/fail.",
        "params": {
          "command": "Command to execute",
          "expected": "Expected string",
          "match_mode": "'contains' or 'exact'. Default: contains"
        }
      },
      "RunBackground": {
        "description": "Spawns a subprocess fire-and-forget. Returns the PID but the output cannot be retrieved.",
        "params": {
          "command": "Command to execute. e.g. 'notepad', 'python script.py'"
        }
      },
      "Kill": {
        "description": "Terminates a background process.",
        "params": {
          "pid": "PID of the process to terminate"
        }
      },
      "ListBackground": {
        "description": "Returns the list of running background processes.",
        "params": {}
      }
    }
  },
  "SHELL": {
    "_description": "Shell Command Execution — Executes Linux/macOS bash commands. Linux counterpart of CMD. Used for running external programs or system commands.",
    "functions": {
      "Run": {
        "description": "Executes a bash command and waits for completion (blocking). Returns the result.",
        "params": {
          "command": "bash command to execute. e.g. 'ls -la /tmp', 'ps -ef | grep python'",
          "timeout": "Max wait time in seconds. Default: 30"
        }
      },
      "Check": {
        "description": "Executes a bash command and verifies the output against an expected string (blocking). Returns a 'FAIL:' prefix on mismatch, which causes the step to be marked as fail.",
        "params": {
          "command": "bash command to execute",
          "expected": "Expected substring or full-match value. Empty = pass only when output is empty (no-output check)",
          "match_mode": "Comparison mode: 'contains' (substring) or 'exact' (full match). Default: contains",
          "timeout": "Max wait time in seconds. Default: 30"
        }
      },
      "Check_Logic": {
        "description": "Executes a bash command and verifies the output against multiple keywords using and/or logic (blocking). Returns a 'FAIL:' prefix on mismatch.",
        "params": {
          "command": "bash command to execute",
          "keywords": "Comma-separated keyword list. e.g. 'OK,ready,done'",
          "logic": "'and' (all keywords required) or 'or' (any one). Default: and",
          "timeout": "Max wait time in seconds. Default: 30"
        }
      },
      "RunCapture": {
        "description": "Executes a command in the background and immediately returns a '[BG_TASK:bg_x]' placeholder (non-blocking).",
        "params": {
          "command": "Command to execute"
        }
      },
      "CheckCapture": {
        "description": "Executes a command in the background and returns a '[BG_TASK:bg_x]' placeholder. On completion, the server evaluates expected/match_mode for final pass/fail.",
        "params": {
          "command": "Command to execute",
          "expected": "Expected string",
          "match_mode": "'contains' or 'exact'. Default: contains"
        }
      },
      "RunBackground": {
        "description": "Spawns a bash subprocess fire-and-forget. Detaches via setsid so it survives caller termination. Returns the PID but output cannot be retrieved.",
        "params": {
          "command": "bash command to execute. e.g. 'sleep 60', 'python3 script.py'"
        }
      },
      "Kill": {
        "description": "Terminates a background process (SIGKILL).",
        "params": {
          "pid": "PID of the process to terminate"
        }
      },
      "ListBackground": {
        "description": "Returns the list of running background processes.",
        "params": {}
      }
    }
  },
  "HKMC6th": {
    "_description": "HKMC 6th-gen IVI device — Screen capture / touch / swipe / HW key control over a TCP socket protocol. Calls the HKMC6thService functions of the connected hkmc_agent device directly.",
    "functions": {
      "tap": {
        "description": "Taps (touches) the given coordinate.",
        "params": {
          "x": "X coordinate (px)",
          "y": "Y coordinate (px)",
          "screen_type": "Screen type. front_center / rear_left / rear_right / cluster"
        }
      },
      "repeat_tap": {
        "description": "Taps the same coordinate N times in a row (for fast double/multi taps).",
        "params": {
          "x": "X coordinate (px)",
          "y": "Y coordinate (px)",
          "count": "Number of taps (default 5)",
          "interval_ms": "Interval between taps in ms (default 100)",
          "screen_type": "Screen type. front_center / rear_left / rear_right / cluster"
        }
      },
      "long_press": {
        "description": "Presses the given coordinate, holds for duration_ms, then releases.",
        "params": {
          "x": "X coordinate (px)",
          "y": "Y coordinate (px)",
          "duration_ms": "Hold time in ms (default 3000)",
          "screen_type": "Screen type. front_center / rear_left / rear_right / cluster"
        }
      },
      "swipe": {
        "description": "Drags (swipes) from (x1,y1) to (x2,y2).",
        "params": {
          "x1": "Start X",
          "y1": "Start Y",
          "x2": "End X",
          "y2": "End Y",
          "screen_type": "Screen type. front_center / rear_left / rear_right / cluster"
        }
      },
      "send_key": {
        "description": "Sends a HW key event (6th-gen keyExt6th packet). See the HKMC spec for cmd/sub_cmd/key_data values.",
        "params": {
          "cmd": "Key category cmd value (e.g. CMD_MKBD/CCP/RRC/SWC/MIRROR)",
          "sub_cmd": "Sub cmd (SHORT_KEY/LONG_KEY/PRESS_KEY/RELEASE_KEY/DIAL_ACTION)",
          "key_data": "Key code (int)",
          "monitor": "Target monitor (0x00=NONE, 0x01=LEFT, 0x02=RIGHT)",
          "direction": "Dial/knob direction byte (if any)"
        }
      },
      "send_key_by_name": {
        "description": "Sends a HW key event by key name (applies spec default + device override).",
        "params": {
          "key_name": "Key name (HKMC_KEYS dictionary key)",
          "sub_cmd": "Sub cmd (default SHORT_KEY)",
          "screen_type": "Target screen",
          "direction": "Dial/knob direction byte (if any)",
          "key_source": "Key source (if any)"
        }
      },
      "screencap": {
        "description": "Captures the current screen and saves it to a file.",
        "params": {
          "output_path": "Save path (PNG/BMP)",
          "screen_type": "Screen type",
          "left": "ROI left (0 if omitted)",
          "top": "ROI top (0 if omitted)",
          "right": "ROI right (screen edge if omitted)",
          "bottom": "ROI bottom (screen edge if omitted)",
          "format": "Save format (png/bmp)"
        }
      },
      "screencap_bytes": {
        "description": "Captures the current screen and returns the bytes (BMP/PNG) without saving to a file.",
        "params": {
          "screen_type": "Screen type. front_center / rear_left / rear_right / cluster",
          "fmt": "Format (png/bmp)",
          "timeout": "Response wait timeout in seconds"
        }
      },
      "get_screen_size": {
        "description": "Returns the (width, height) size of the given screen.",
        "params": {
          "screen_type": "Screen type. front_center / rear_left / rear_right / cluster"
        }
      },
      "ethernet_signal": {
        "description": "Sends an Ethernet signal (payload + signal_id).",
        "params": {
          "n_signal_payload": "Signal payload value",
          "n_signal_id": "Signal ID"
        }
      },
      "execute_shell": {
        "description": "Sends a shell command packet (low-level — specify cmd/sub_cmd/resp directly).",
        "params": {
          "cmd": "Packet cmd",
          "sub_cmd": "Sub cmd",
          "resp": "Response code",
          "str_data": "String to send"
        }
      },
      "send_shell_cmd": {
        "description": "Sends a simple shell string command to the device.",
        "params": {
          "str_data": "Command string to send"
        }
      }
    }
  },
  "Android": {
    "functions": {
      "StartLogging": {
        "description": "Start capturing logcat for the given Android device (in-memory buffer). If clear=True, flushes the logcat ring buffer first. Auto-restarts logcat into the same buffer if the device reboots/disconnects.",
        "params": {
          "serial": "Target ADB device serial. Empty = derived from the step's device",
          "clear": "Clear logcat buffer before starting. true/false (default true)"
        }
      },
      "StopLogging": {
        "description": "Stop logcat capture and save the buffer to a file ({run_dir}/logs/logcat_*.log during playback, results/Temp_logs/ for step tests).",
        "params": {
          "serial": "Target ADB device serial. Empty = derived from the step's device",
          "save_path": "Save path. Empty = automatic path"
        }
      },
      "ClearLog": {
        "description": "Clear the device's logcat ring buffer (adb logcat -c).",
        "params": {
          "serial": "Target ADB device serial. Empty = derived from the step's device"
        }
      },
      "Monitor_pass_on_keyword": {
        "description": "PASS if keyword is found in the captured logcat. If include_past=True, scans logs captured so far first, then monitors new lines for `time` seconds. Requires StartLogging first.",
        "params": {
          "keyword": "Keyword to PASS on (substring match)",
          "time": "Seconds to wait for future log lines. Default 5. 0 = past buffer only",
          "serial": "Target ADB device serial. Empty = derived from the step's device",
          "include_past": "Whether to also scan logs captured so far. true/false (default true)"
        }
      },
      "Monitor_fail_on_keyword": {
        "description": "FAIL if keyword is found in the captured logcat. For monitoring abnormal keywords (FATAL/ANR/Exception). Scans past buffer (if include_past) and new lines for `time` seconds.",
        "params": {
          "keyword": "Keyword that triggers FAIL (substring match)",
          "time": "Seconds to monitor future log lines. Default 5. 0 = past buffer only",
          "serial": "Target ADB device serial. Empty = derived from the step's device",
          "include_past": "Whether to also scan logs captured so far. true/false (default true)"
        }
      }
    }
  },
  "Frame_Check": {
    "_description": "Operation-time measurement tool based on the test recording video (webcam). Add a measurement start point (MeasureStart) and a target-image point (MeasureEnd) step to the scenario; when the scenario ends, webcam recording stops automatically and the video is analyzed frame by frame to report the elapsed time (ms) from the start point to the first appearance of the target image. The PIP webcam must be open before playback for recording/measurement.",
    "functions": {
      "MeasureStart": {
        "description": "Records the measurement start point. With mode='function' the moment this step executes is the start point; with mode='image' the start point is the first video frame (after this step executes) where the given image appears. Use the edit button to crop the current PIP webcam frame.",
        "params": {
          "mode": "Start-point basis: 'function' (step execution time) or 'image' (first frame the image appears)",
          "image": "Start-point image (required when mode='image'). Set by cropping the webcam frame via the edit button",
          "threshold": "Image match similarity threshold (0.0~1.0, default 0.8)"
        }
      },
      "MeasureEnd": {
        "description": "Records the target-image point. After the scenario ends, the recording is scanned starting from this step's execution time (earlier frames are skipped) for the first frame where the target image appears, and the elapsed time (ms) from the start point is reported. Scanning stops after max_time seconds. Use the edit button to crop the current PIP webcam frame.",
        "params": {
          "image": "Target image (required). Set by cropping the webcam frame via the edit button",
          "threshold": "Image match similarity threshold (0.0~1.0, default 0.8)",
          "max_time": "Maximum scan time in seconds. If the target is not found within this window from the scan start point, it is reported as not found. 0 = unlimited (default 60)"
        }
      }
    }
  }
}
